{
  "name": "fiis-generador-horarios",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "build:data": "esbuild scripts/build-defaults.ts --bundle --platform=node --format=cjs --outfile=node_modules/.cache/build-defaults.cjs --log-level=warning && node node_modules/.cache/build-defaults.cjs"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^3.2.4"
  }
}
